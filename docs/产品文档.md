# CompeteScope 企业级竞品分析 AI Agent 平台

## 产品经理（PM）全链路全景理解与产品主导交接白皮书

## 📌 前言：致我们的首位产品经理（Owner 寄语）

欢迎加入 CompeteScope 核心团队！

这份文档由前期的技术与架构发起人共同整理。必须向你坦诚的是，**由于此前团队中缺乏专业的产品经理，这份文档和现有的规划带有浓厚的技术视角、研发偏好以及不成熟的业务直觉。** 它在底层技术可行性、Agent 编排逻辑上是坚实的，但在**严肃的商业闭环、用户共情体验、长效的 PM 指标体系以及严密的需求边界控制**上，存在大量亟待职业产品经理修正、推翻并重新定义的内容。

我们不希望你仅仅将这份文档视为“被动接受的既定计划”，而是希望你将其作为**第一份审计靶子**。请充分发挥你的专业产品思维，挑战我们现有的假设，找出规划中的漏洞，重新梳理功能优先级，并全面接管、修改、重构这份全链路文档与后续的迭代路线图。以下是项目的全景现状，请你开始掌舵。

## 1️⃣ 项目背景、商业痛点与未竟之问

### 1.1 核心痛点与行业现状

在当前的 B 端企业和互联网出海团队中，竞品动态监测呈现出“高频次、深维度、高时效”的刚需。然而，传统的竞品分析面临三个核心痛点，这也是技术团队尝试构建 CompeteScope 的出发点：

- **信息滞后与海量噪声**：依赖人工手动检索，极易遗漏散落在垂直社区、技术博客或海外论坛（如 Reddit、Product Hunt）的微弱信号（如竞品的一条悄悄更新的 Changelog）。
    
- **边际成本过高**：一份涵盖功能拆解、定价策略及 SWOT 矩阵的深度研报，需要消耗高阶分析师 4 到 8 小时。企业受限于人力，无法对市场上数十个潜在对手进行全量、长期的跟踪。
    
- **通用大模型的“商用幻觉”**：直接用 ChatGPT 或 Perplexity 进行竞品提问，答案往往是非结构化的文本，且模型基于过时知识库进行“编造”，缺乏严肃商业评审所必需的**数据出处（Citation）与结构化可对比性**。
    

### 1.2 现有业务目标与 KPI（亟待你进行定量与精细化）

技术团队初步设定的项目阶段性 KPI 如下，我们非常需要你从商业可行性角度，重新评估这些指标是否合理，以及如何通过产品手段进行数据埋点和达成：

- **交付时效（Efficiency）**：端到端（从输入名字到出具深度研报）的总时间控制在 **10 分钟以内**。
    
- **研报一阶采纳率（Adoption Rate）**：目标企业客户在实际业务决策中，无需大幅人工修改即可直接采用的报告比例达到 **75% 以上**。
    
- **单份报告 Token 成本**：通过工程优化，将单次 Multi-Agent 协同的 LLM Token 成本控制在 **$1.50 美元以内**。
    

### 1.3 初始用户画像（Persona）

目前我们粗略地将目标用户划分为以下三类，请你入场后通过深度的用户调研，重新修正和丰富其行为特征：

1. **产品总监 / 资深 PM**：在产品立项、年度战略规划前，需要快速摸清赛道标杆产品底细的决策者。
    
2. **市场情报分析师 (CI Analyst)**：需要定期输出行业竞品动态，监测对手定价、融资和海外声量的专业执行者。
    
3. **早期创始人 / 投资经理**：需要在 10 分钟内建立对某个垂直技术赛道全景认知的快速评估者。
    

### 1.4 PM 破局点：需要你重塑的“成功定义”

研发团队往往容易陷入“技术成功（即流程跑通、出字多）”的自嗨中。**我们需要你来定义什么是“商业成功”与“产品成功”**：报告的内容深度是否真的切中产品总监的痛点？SWOT 矩阵的分类逻辑是否符合标准的商业模型，而非 AI 的生搬硬套？请你接管后，立刻建立一套**核心内容质量评判标准（Evaluation Rubric）**。

## 2️⃣ 功能版图、动态优先级与需求重构

作为一款基于 LangGraph 构建的 Multi-Agent 工作台，技术团队对功能的划分带有明显的“技术实现难度偏好”。作为专业 PM，请你从用户价值和 MVP（最小可行性产品）视角，重新对以下功能进行**砍需求、排先后、调权重**。

### 2.1 初始功能分层规划（供你审计与修改）

- **核心功能点（技术团队拟定的 P0 边界）**：
    
    - _单点任务配置表单_：用户输入目标产品名，勾选核心分析维度（功能/定价/定位/SWOT）。
        
    - _异步 Multi-Agent 编排（LangGraph）_：由 Planner $\rightarrow$ Research $\rightarrow$ Analysis $\rightarrow$ Writer $\rightarrow$ Reviewer 五个核心 Agent 节点串联组成的异步工作流。
        
    - _流式进度画布（DAG Visualizer）_：前端利用 SSE 协议实时高亮当前正在工作的 Agent，并暴露其正在调用的工具流。
        
    - _结构化报告视图_：Markdown 渲染页面，包含 SWOT 四象限图表与定量卡片。
        
- **可选功能（技术团队拟定的 P1 边界）**：
    
    - _Agent Trace 可视化_：向高级用户开放，展示每个节点的 Token 消耗、耗时轴及详细的 Tool Input/Output。
        
    - _动态网页抓取（Firecrawl）_：针对竞品官网进行深度、防爬、结构化的正文提取。
        
    - _Citation 溯源悬浮面板_：报告文本中的数据附带数字上标，鼠标悬浮可查看原始 URL 及上下文快照。
        
- **长期演进功能（技术团队拟定的 P2 边界）**：
    
    - _Delta 增量监测订阅_：一键关注某竞品，系统每周自动运行，仅抓取过去 7 天的新闻、更新日志和定价变动，输出差量对比周报。
        
    - _Human-in-the-loop (HITL) 人工干预机制_：AI 报告生成后进入待审核状态，允许企业内部专家进行一键驳回、文字修正或卡片指标微调。
        

### 2.2 初始用户故事（User Stories 粗胚）

- _故事 1_：作为一个市场分析师，我希望输入“Linear”并勾选“定价策略”，系统能针对性地帮我搜集相关信息，而不是产生一堆无用的泛泛内容。
    
- _故事 2_：作为一个高管用户，在长达 5 分钟的报告生成期间，我需要清晰地知道“系统在干什么、当前在看哪些网站”，以便我建立对产品的信任并保持耐心。
    
- _故事 3_：作为一个需要向 CEO 汇报的产品总监，我希望研报中的每一处关键结论（如“Notion 在 2025 年推出了企业级安全套件”）都附带权威的数据出处链接，避免我在商业评审中遭到专业质疑。
    

### 2.3 PM 破局点：如何防止技术团队的“需求蔓延”与“炫技”？

目前研发团队在 `P1` 中规划了极其复杂的 `Agent Trace 可视化`（展示 Token 和 Tool 日志）。**请你从产品角度判断**：普通 B 端用户真的需要看这么技术底层的 Trace 吗？这是否会干扰用户的核心视线？我们是否应该将其归入内部 Debug 工具，或者作为企业版的高级客制化功能？请你对需求边界重降砍刀。

## 3️⃣ AI-Native 用户体验、交互链路与共情设计

CompeteScope 是一款 **AI-Native（AI原生）** 的工作台产品。它不是传统的“表单加表格”系统，而是“用户启动一个任务，AI 异步执行数分钟，最终交付复杂成果”的形态。这里存在大量的 UX 心理学空白需要你来填补。

### 3.1 初始核心用户操作路径（User Flow）

```
[ 1. Dashboard 首页 ] ──▶ 点击新建 ──▶ [ 2. 任务配置表单 ]
                                                 │ 提交任务 (异步创建)
                                                 ▼
[ 4. 报告查看页 ] ◀─── 自动跳转 ◀─── [ 3. 任务执行详情页 ]
  - SWOT 矩阵 / 指标卡片               - SVG 线性五节点拓扑图
  - Markdown 报告内容                  - 实时 Tool Call 动态和日志流
  - Citation 溯源面板                  - 耗时与 Token 计数器
```

### 3.2 页面架构与视觉锚点现状

- **Task Dashboard**：展示历史任务列表、当前状态（成功/执行中/失败）、创建时间。支持一键重试失败的任务。
    
- **Task Run Workspace (`/tasks/[id]`)**：这是目前的技术视觉核心。上方为手绘的五节点线性 DAG 图，节点颜色随状态流转（等待/运行脉冲/成功打勾/失败叉号）。下方为流式的折叠日志流，展示 Agent 的思考 Message。
    
- **Report Detail (`/reports/[id]`)**：顶部为三个高亮定量卡片（如：威胁等级、估算用户规模），中部左侧为标准 SWOT 四象限图，右侧为 Markdown 渲染的深度报告，右侧边栏为 Citation 溯源面板。
    

### 3.3 PM 破局点：AI 异步等待期（长达数分钟）的心理学共情设计

目前研发通过 SSE（Server-Sent Events）把底层的 Agent 执行日志粗暴地推给了前端。**这正是非专业产品设计的典型漏洞。** 用户看到一堆像代码一样的日志会感到困惑。

**请你对其进行重新设计**：如何将冷冰冰的“Tool Call: Tavily Search”转化为富有商业同理心的“AI 正在检索 Linear 的海外官方博客...”、“AI 正在为您梳理 2025 年最新的定价变更条款”？如何通过精妙的文案和微交互，消除用户在数分钟等待期内的焦虑感？这需要你立即介入。

## 4️⃣ 技术架构全景、Agent 编排与产品经理的底线思维

掌握底层的技术架构，能帮助你更精准地评估开发工期，并与研发团队在同一语境下对话。

### 4.1 全 Python 后端架构（FastAPI + LangGraph）

系统在 v3.0 架构中进行了极致的精简，打破了传统的“前端 Node + 后端 Python”的跨语言微服务架构。目前采用**统一的 Python 后端服务**，将 **FastAPI API 网关** 与 **LangGraph Agent Runtime** 合并在同一个 Python 进程中，通过线程池实现异步任务调度。

### 4.2 系统三层分层架构图

如上图所示：

1. **前端层（Frontend Layer）**：Next.js 14 + TailwindCSS + Shadcn UI，利用标准 Web API `EventSource` 连接后端的 SSE 端口，实现基于状态流转的视图点亮。
    
2. **后端层（Backend Layer）**：FastAPI 持有 REST 接口与 SSE 长连接。LangGraph 引擎异步驱动状态机，并通过 Tool Ecosystem Layer（工具层）调用外部服务。
    
3. **数据与事件总线层（Data & Event Layer）**：PostgreSQL 存储最终的结构化资产；Redis Pub/Sub 担当底层的事件分发中心，确保在后续多实例水平扩展时，前端连接能 100% 捕获跨进程的 Agent 动态事件。
    

### 4.3 核心模块：LangGraph Multi-Agent DAG 协同流

系统底层的核心是一个由有向无环图（DAG）定义的有状态机。5 个专职 Agent 节点在运行时共同维护并读写一个全局的 `AnalysisState` 上下文：

```
[START] ──▶ 1. Planner Agent ──▶ 2. Research Agent ──▶ 3. Analysis Agent
                                      │                     │
                                      ▼ [条件边重试]          ▼ [条件边重试]
                                信息少于5条? 重试      JSON解析失败? 重试
                                      │                     │
                                      ▼                     ▼
[END] ◀─── 5. Reviewer Agent ◀────────────── 4. Writer Agent
                │                                   │
                ▼ [条件边重试]                        ▼ [条件边重试]
          评分<0.8? 驳回重跑                    字数<500字? 重试
```

- **Planner**：解析用户定制的维度，推导出 3-5 个具体的、带有精确时效年份（如 2025-2026）的中英文高级搜索词。
    
- **Research**：通过 `ToolRouter` 并发调用 Tavily 搜索和 Firecrawl 抓取，清洗原始 HTML，提炼并返回结构化的 `ResearchItem` 数组。
    
- **Analysis**：核心战略分析节点。根据 Prompt 规则提炼 SWOT 矩阵，并强行约束模型必须输出符合 **JSON Schema** 的高度结构化商业字段（严禁输出散落的纯文本）。
    
- **Writer**：将 Analysis 的 JSON 数据转化为高商业感的 Markdown 报告，并在特定 HTML 注释中埋入定量卡片数据。
    
- **Reviewer**：充当底层质量网关，对内容完备度进行 0.0 - 1.0 的打分，不达标则触发条件边强行让上游节点重跑。
    

### 4.4 PM 破局点：输入边界控制（Prompt 防御性设计）

研发团队在编写 Prompt 时，往往追求大而全。**我们需要你以专业产品视角介入核心 Prompt 的联合评审（Prompt Code Review）**：

- 当用户输入一个极其边缘或根本不存在的产品名时，Planner 是否会胡编乱造？
    
- 如何从产品机制上杜绝 Analysis 节点将两个完全不相关的竞品特征混淆在一起？
    
- 你需要梳理并提供一套《极端恶意/无效输入边界清单》，从产品层面指导研发在最上游进行 Prompt 拦截与入参校验。
    

## 5️⃣ 数据工程、商业指标体系与冷启动监控

### 5.1 数据流向与核心存储表

目前后端的数据库设计（基于 SQLAlchemy + Alembic）主要围三张核心表展开，结构完全透明，等待你提出新的数据维度扩充：

- `tasks`（任务表）：记录 `task_id`、任务当前所处状态、用户的初始配置（JSON）、启动与结束时间、以及因外部 API 崩溃捕获的 Error Trace 堆栈。
    
- `reports`（报告表）：存储最终的 Markdown 文本、SWOT 结构化 JSON、以及用于前台 Citation 悬浮面板展示的 `citations_data` 引用 URL 数组。
    
- `execution_logs`（执行日志表）：记录细粒度到节点级的 `duration_ms`（耗时）、事件类型（`agent_start`/`tool_call`）以及每一步的 Token 消耗计数。
    

### 5.2 现有技术监控指标（现有的粗粒度指标）

技术团队在 Grafana 监控面板上目前只拉取了以下几项粗糙的指标：

- **Task Success Rate**：$\frac{\text{COMPLETED 状态的任务数}}{\text{总提交任务数}}$。
    
- **Avg End-to-End Latency**：端到端平均总耗时轴。
    
- **JSON Parse Failure Rate**：Analysis 节点由于大模型偶发性输出格式错误导致 JSON 解析失败的概率。
    

### 5.3 PM 破局点：如何建立数据驱动的 Prompt 迭代漏斗？

**这是非专业产品设计暴露出的最大空白。** 目前团队完全不知道用户对生成的报告到底满不满意。

**请你立即入场修改这个设计**：我们需要在报告页面引入哪些用户反馈机制（例如：点赞/点踩、一键复制、一键导出、针对单条结论的修改反馈）？如何将这些用户行为数据沉淀到 `reports` 表中，从而建立一个反馈闭环（Feedback Loop），用来指导技术团队对底层 Prompt 进行数据驱动的精准迭代？这迫切需要你的规划。

## 6️⃣ 敏捷迭代计划、人力矩阵与应急预案

项目当前处于高压的 20天 MVP 冲刺周期中，使用的是精益创业与 Vibecoding（AI辅助编码）相结合的方式。

### 6.1 20天 MVP 每日开发计划与里程碑

```
[ Week 1: 核心 Pipeline 跑通 ] ──────▶ [ 里程碑 1: Day 4 ] CLI 端到端出报告
                                      ▶ [ 里程碑 2: Day 6 ] 浏览器全流程跑通
                                                 │
                                                 ▼
[ Week 2: 企业级特性增强 ]     ──────▶ [ 里程碑 3: Day 12 ] 三大案例质量完美对齐
                                      ▶ [ 里程碑 4: Day 14 ] 录制首版 Demo 视频
                                                 │
                                                 ▼
[ Week 3: 演示冲刺与封版 ]     ──────▶ [ 里程碑 5: Day 20 ] 系统正式上架/上线 🎉
```

### 6.2 团队初始分工矩阵

- **产品经理 (PM - 你)**：接管并修正本白皮书、卡死功能边界、设计全新的用户体验/共情反馈、输出测试案例。
    
- **架构/Agent 工程师 (C)**：主攻 `backend/agents/`，负责重构 LangGraph 状态机及调优 Prompt。
    
- **后端工程师 (B)**：负责 FastAPI 接口设计、PostgreSQL 数据库增删改查、Redis 事件通信总线部署。
    
- **前端工程师 (A)**：负责 Next.js 工作台搭建、手绘 SVG 的 `DAGVisualizer` 实时高亮、Markdown 及图表渲染。
    

### 6.3 PM 核心预案：Demo 现场的“黑天鹅”降级策略（三级防御）

大模型的最大特点是其不确定性。在公司严肃的评审会、发布会或面对投资人进行产品 Demo 演示时，API 极易发生网络拥堵或崩溃。**技术团队之前没有做任何降级预案，这非常危险。请你作为 PM 熟记并严格执行以下三级防御机制**：

- **Level 1（轻度风险防御）：演示模式缓存路由（Demo Cache Mode）**
    
    - _具体做法_：请你在 `/tasks/new` 提交端点处增加一个硬性设计。如果是内部演示或开启了“演示模式”，当前端捕获到标准的演示关键词（如“Notion”、“Linear”、“Figma”）时，后端不真正调用大模型和实时搜索。而是**直接短路路由，从数据库中秒级拉取预先跑好的、完美的、绝不报错的静态全量报告**，实现 0 延迟秒出。确保演示现场的绝对流畅。
        
- **Level 2（中度风险防御）：全局调用熔断与残缺降级（Circuit Breaker）**
    
    - _具体做法_：当系统在真实的未知域名下运行，由于 LLM 格式 Bug 导致 Analysis 与 Reviewer 节点发生死循环重试时，严禁让前端卡死或无限转圈。在 `AnalysisState` 中定义全局调用计数器。一旦 `llm_call_count` 超过 15 次，**强制终止有向图流转，流向兜底节点 `FallbackNode`**。利用现有的残缺 Research 素材强行拼装一份“带有温和警告提示的研报”，将任务标记为 `COMPLETED_WITH_WARNINGS` 并在前端软着陆展现，绝对不能在用户面前抛出红字崩溃。
        
- **Level 3（严重风险防御）：全服务商宕机崩溃（Total Blackout）**
    
    - _具体做法_：如果发生大模型服务商全球大面积宕机（如 DeepSeek/OpenAI 100% 拒绝服务），在底层通过 LiteLLM 统一抽象层配置一键切向 Azure OpenAI 或 Anthropic 备份接口。同时，你必须随身携带一份 **6 分钟的录屏完美操作视频（Walkthrough Video）**，作为现场演示的最后底线。
        

## 7️⃣ 流程机制、联合评审与变更管控

### 7.1 前后端与 Agent 协议的“三驾马车”变更流程

在一个 Multi-Agent 项目中，数据的流动是全链路强耦合的。任何一个技术节点的私自改动都会引发系统性塌方。因此，你入场后必须推行严格的**变更管控流程**。以下三个核心文件被定义为项目的“核心圣经”，未经你和全队联合评审，任何开发人员严禁私自修改一行代码：

1. `backend/core/state.py`：定义了 Agent 之间传递的共享状态上下文（AnalysisState）。改动它意味着所有 Agent 的 Prompt 输入输出结构全部失效。
    
2. `backend/schemas/events.py` 与 `frontend/types/sse-events.ts`：定义了前后端 SSE 异步事件交互的契约。一旦不一致，前端组件将无法点亮当前运行状态。
    
3. `docs/api-contract.md`：API REST 契约文档。
    

### 7.2 研发分支与 Commit 契约

代码提交必须严格遵循业务标签规范：

- `feat(agent): 实现 Research Agent Tavily 搜索集成`
    
- `fix(agent): 修复 JSON 解析失败未触发重试的 Bug`
    
- `prompt(agent): research v2 增加多关键词搜索策略`
    

### 7.3 QA 与 PM 联合验收机制（Three-Case Regression）

在每次 Prompt 或 Workflow 进行重大版本合并前，你必须带队执行“三标准案例回归测试”。确保在 **Notion（协同办公）、Linear（项目管理）、Figma（设计工具）** 这三个标准测试产品下，系统能够完美达成：

1. 端到端流程跑通，不触发熔断；
    
2. Reviewer Agent 最终审计评分 $\ge 0.8$；
    
3. SWOT 矩阵和定价数据中未发生明显的常识性交叉混淆（即未把 Notion 的功能安在 Figma 身上）。
    

## 8️⃣ 商业护城河、竞争壁垒与长效价值

技术人员容易专注于功能实现，而你需要专注于**商业壁垒的构建（Moat Building）**。

### 8.1 相比传统 Chat/Perplexity 的差异化路径

如上图所示，我们在向市场和投资人讲述产品故事时，必须强调用工程化的确定性来对冲大模型的不确定性。我们的核心壁垒由以下四个支柱构成：

- **结构化输出驱动（Structured Data-Driven）**：我们交付给用户的不仅是可读的 Markdown 研报，更是后端强行提炼出的、符合 JSON Schema 的商业属性数组。这意味着我们的数据可被二次结构化对比、可直接接入 B 端的监控仪表盘（Dashboard）。
    
- **白盒可观测性（Radical Transparency）**：传统的 AI 工具是黑盒。我们通过前台的实时拓扑图和 Tool Call 日志，将 AI 的“搜集、推理、多轮修正、自我审计”过程完全透明地展现给 B 端客户。这种“过程可见性”在严肃的商业场景中能够极大地建立起产品的信任度。
    
- **确定性逻辑编排（LangGraph Determinism）**：对比原生的 AutoGPT 或完全自主路由的 Agent 极易跑偏、陷入死循环、成本不可控的特点，我们用 LangGraph 将商业研报的严谨生产路径（拆解-检索-分析-执笔-审计）进行了确定性的工程框架卡死，实现了大方向绝对可控、小节点自主重试。
    
- **完备的知识溯源（Citation System）**：每一条核心结论均深度绑定原始数据清洗后的 URL 指纹，彻底解决 B 端落地的幻觉危机。
    

### 8.2 PM 破局点：如何规划从“单点研报”向“持续监测（Delta）”的商业闭环？

**这是目前技术架构设计最大的隐患。** 现在的逻辑是：用户输入名字 $\rightarrow$ 等 10 分钟 $\rightarrow$ 出报告，这是一个典型的“单点触发”工具，用户用完就走，留存率（Retention）会非常低。

**请你作为产品主导，立刻着手修改后续的产品路线图**：如何尽快推进 `P2 的 Delta 增量监测订阅`？企业真正需要的是一个“守门人”——我配置一次 Notion 之后，平台每周自动帮我跑 Research Agent，**只抓取过去 7 天内 Notion 官网、Twitter 社区和海外博客的微弱差量变化**，并与上周报告做 Diff 对比，在每周一晨会前推送一份《Notion 本周动态差量对比简报》。**只有这个功能跑通，产品才能实现从“低频工具”向“高频 B 端高粘性 SaaS”的惊人质变。** 我们期待你在完成 MVP 版本后，立刻拿出这部分的详细 PRD。

## 9️⃣ 严肃 B 端底线：合规、安全与隐私防御

技术团队在开发 Demo 时，往往完全忽略了安全合规。如果我们要将产品卖给中大型企业，合规是决定能否通过企业采购（Procurement）审计的生死线。

### 9.1 多租户与客户数据隔离（Multi-Tenancy）

- **技术漏洞现状**：目前数据库模型中的 `tasks` 和 `reports` 表仅以自增的 `id` 或 `task_id` 为主键，**完全缺失了租户（Tenant ID）或用户维度（User ID）。**
    
- **请你立刻强制要求研发修改**：在严肃的 B 端场景下，A 公司的分析师正在调研某个极其敏感的战略竞品或自身缺陷，如果由于越权漏洞或缺乏隔离导致 B 公司的员工看到了这份任务和报告，将引发毁灭性的合规与法务灾难。数据库模型必须全面引入 `tenant_id`，所有后端的 CRUD 操作必须在中间件层强制带上作用域过滤器和行级安全（Row-Level Security）。
    

### 9.2 网页抓取合规性与反爬法务风险

- **技术漏洞现状**：研发团队计划让 Research Agent 通过 Firecrawl 直接去高频高并发地抓取竞品的官网、Changelog 页面。这极易被对方防火墙判定为 DDoS 攻击，导致我们平台的公网 IP 被集体封禁，甚至引发知识产权侵权纠纷。
    
- **请你作为 PM 制定以下硬性抓取治理规则（Guardrails）**：
    
    1. _商业住宅代理池集成_：强制要求后端集成动态代理池（Proxy Pool），实现每次 Tool Call 抓取时自动轮换 IP。
        
    2. _Robots.txt 尊重机制_：在代码的最上游建立规则，抓取前必须自动校验目标域名的 `robots.txt`，严禁强行抓取对方明确标记为 `Disallow` 的私有数据目录。
        
    3. _单域名频率限制（Rate Limiting）_：设置单分钟内的抓取频率上限，做到对目标网站的“文明抓取”，确保平台的合规可持续运行。
        

### 9.3 客户输入与隐私保护（Data Privacy & GDPR）

- **产品合规布局**：部分企业用户可能会在自定义维度表单中误输入其公司内部未公开的敏感数据。你需要在服务协议和前端显著位置进行合规布局：
    
    1. 明确声明我们的底层 API 接口均走的是商业企业级渠道（API 渠道），根据大模型服务商（OpenAI/DeepSeek 等）的合规条款，**明确承诺通过 API 传输的数据绝对不会被用于底座模型的二次训练**。
        
    2. 前端输入框下方增加安全 Tip 提示：“请勿在表单中提交包含企业未公开的核心机密、个人敏感隐私或受到 GDPR 保护的数据”。
        

## 🛠️ 附件：产品经理（PM）首月审计与修改专项 Backlog

为了让你能够以最快的速度深度参与进来并全面接管项目，我们为你梳理了以下这份**即刻启动的修改与落地任务清单（Action Items）**。整个研发团队已经严阵以待，随时准备配合你的修改：

|**序号**|**模块**|**核心修改任务与你的专业产出物**|**研发对接人**|
|---|---|---|---|
|1|**UX/UI 交互**|重新梳理 `/tasks/[id]` 页面的 **SSE 实时流式日志文案**。请将冷冰冰的、具有程序员特征的代码级日志，全部重写为具有高度专业商业感、用户易懂的“AI 动态思考文案”。|工程师 A（前端）|
|2|**Prompt 评审**|深度参与 `backend/agents/analysis.py` 的 **Prompt Code Review**。请站在产品视角，在 Prompt 中强制约束大模型在输出 SWOT 研报时必须遵循的商业逻辑标准，并提供首批《无效/恶意输入拦截清单》。|工程师 C（Agent）|
|3|**安全合规**|监督并推动后端在 `tasks` 和 `reports` 数据库模型中强行加入 `tenant_id` 与 `user_id` 字段，并制定前端在 JWT 鉴权状态下的行级安全隔离逻辑。|工程师 B（后端）|
|4|**降级预案**|联合前端与后端，在 `/tasks/new` 页面和核心状态机中，全面落实本白皮书 6.3 节规划的**“三级降级预案（演示缓存模式与全局死循环计数熔断）”**的代码级实现。|全体研发团队|
|5|**商业路线图**|针对 P2 的 **“Delta 差量监测订阅周报（Notion / Linear 案例）”**，在首周内输出第一版面向中大型 SaaS 客户的详细 PRD 功能拆解与交互初稿。|PM 独立主导|

这份文档是技术团队交付给你的初始拼图，我们深知其中的不完美。**现在，这个项目的全局视野和核心细节决定权已经交到了你的手中，请开始对它进行大刀阔斧的修改与完善！**